version: '3.8'

services:
  saai-frontend:
    build: .
    container_name: saai_production_frontend
    ports:
      - "8085:80"
    restart: always
    environment:
      - VITE_SUPABASE_URL=${PRODUCTION_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${PRODUCTION_SUPABASE_ANON_KEY}
